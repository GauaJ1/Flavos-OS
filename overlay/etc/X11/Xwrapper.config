# Xwrapper.config — Flavos OS
# Permite que startx (sem Display Manager) acesse hardware de input e vídeo.
# Necessário porque não usamos LightDM/GDM — o X é lançado diretamente do TTY.
allowed_users = anybody
needs_root_rights = yes
